===============================================================
 START-CLAUDE-CLOUD -- WHAT TO PASTE INTO A NEW CLOUD CHAT
 Dated: 2026-08-12 17:05 ET
 Was: START-HERE.txt (2026-08-10), then Check-Claude-Cloud.txt
 (renamed to Start-Claude-Cloud.txt 2026-08-12, to pair with Start-CC.txt)
===============================================================

Open a new chat at claude.ai, then copy the block between the
lines below and paste it as your first message.

You do not need to change anything in it. No filenames, no
dates, no build number. That is the point of this version.

THIS IS THE CLAUDE CLOUD VERSION. For Claude Code in a terminal
use Start-CC.txt instead -- it adds Get-Date, the working-root
check and git state, none of which Cloud can do.

---------------- COPY FROM HERE ----------------

Read ProjectDocs/CURRENT.md from the GitHub connector. It names the
current version of every document. Then read the briefing and the
session log it names, and read CLAUDE.md.

If there is no GitHub connector in this conversation, say so and
stop -- do not answer from memory or from project knowledge. Say
"cannot open the file", not "cannot find it"; they are different
problems. If I paste content to you instead, that is fine -- use
it, and say you are working from pasted text.

First, before anything else: find the exact phrase "local account
named panther" in the briefing and quote me the WHOLE sentence it
sits in. The phrase, not just the word. If you cannot find it, you
are reading an old snapshot -- say so and stop.

Then tell me, before doing any work:
1. The current build number and whether it has been field run.
2. The top three open items.
3. Anything you checked and found already stale.

Do not start work until I tell you what we are doing.

----------------- COPY TO HERE -----------------

---------------------------------------------------------------
 WHY THIS FILE CHANGED ON 2026-08-12
---------------------------------------------------------------

It used to say: read ProjectDocs\_READ-FIRST-Briefing-*.md and
take the newest by the date in the filename.

CLOUD CANNOT DO THAT. It cannot list a folder and it cannot sort
one. With five briefings in ProjectDocs it searched by relevance,
took whichever ranked highest, and reported that as success. On
2026-08-12 it told Bill it could not find the current files at
all -- while every one of them was tracked and on origin/main.

The instruction was correct for Claude Code, which can glob, and
impossible for Cloud, which cannot. Nobody noticed the asymmetry
because the failure was silent: Cloud always found A briefing.

THE FIX: ProjectDocs/CURRENT.md. One filename that never changes,
listing the filenames that do. Cloud reads one name it was told
and gets valid pointers.

CURRENT.md IS GENERATED, NEVER TYPED -- by Tool\Update-Current.ps1,
run at session end. A pointer kept by hand is the exact failure it
exists to prevent: 15 of 49 filename references in this project had
already gone dead. It refuses to write at all if any document is
missing, rather than publish a list with a hole in it.

WHY THE PANTHER LINE: that sentence exists only in documents dated
2026-08-11 or later. If Cloud can quote it, it is genuinely reading
current files. If it cannot, it is on an old snapshot -- a different
problem with a different fix, and worth knowing before you trust
anything else it says.

IT ASKS FOR THE PHRASE, NOT THE WORD, AND THAT MATTERS. Measured
2026-08-12: "panther" alone appears 23 times across 14 files,
including two dated 2026-08-03 and 2026-08-05 where it is simply
Bill's name on a document header -- "Prepared for: Panther (William
F. Burns III)". Cloud searches by relevance. Asked for the word, it
could land on a file eight days stale, quote it confidently, and
PASS A TEST THAT PROVED NOTHING -- which is the exact failure this
test exists to catch, wearing the test's own clothes.

The phrase "local account named panther" appears ONLY in documents
dated 2026-08-11 or later. That is what makes it a discriminator.

THE GENERAL RULE: a freshness check is only worth as much as the
uniqueness of the string it asks for. Before trusting one, grep the
whole tree for that string and confirm every hit is in a current
file. If any hit is old, the check can pass while being wrong.

---------------------------------------------------------------
 FIRST LIVE RUN, 2026-08-12 -- CHECK THE CONNECTOR FIRST
---------------------------------------------------------------

Cloud refused to answer, correctly. It measured before concluding --
/mnt/project empty, no CURRENT.md, no ProjectDocs, no .git, no
uploads, and NO GITHUB CONNECTOR in the conversation -- then said
"Stopping. I cannot read the repository" and answered none of the
three questions, because every answer would have come from memory.

THAT SETTLES THE 2026-08-11 FAILURE. It was blamed on the glob
instruction, with a stale connector index as second suspect.
NEITHER. There was no connector in the conversation at all.

A6 passed 2026-08-11 at 14:45, so one existed then. So either the
chat was opened OUTSIDE the GatewayGuard project, or the connector
has been removed since.

BEFORE DIAGNOSING ANYTHING ELSE: confirm the chat was started
inside the project, and that the project still lists the GitHub
connector. A plain claude.ai chat has neither.

WHY IT ASKS FOR ITEM 3: every briefing so far has gone stale within
hours, including two pointers in one written the same afternoon.
Asking for it up front turns a silent trap into the first thing you
are told.

---------------------------------------------------------------

If you only remember one line, this is enough:

    Read ProjectDocs/CURRENT.md and CLAUDE.md, quote the panther
    line to prove you are current, then tell me the build, the top
    3 open items, and what is stale.
